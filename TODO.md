# TODO

- Add a device-side reset gesture: six taps should trigger a hardware reset by pulling `RST`.
- Add firmware support for `ESP32-S3` while keeping the same web configurator and serial protocol.
